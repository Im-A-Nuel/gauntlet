{
  "name": "gauntlet-demo-repo",
  "private": true,
  "version": "0.0.0",
  "description": "Curated TS pricing/cart service used to demo Gauntlet's Trust Score. Baseline tests (test/weak) are deliberately shallow; test/strong is a hand-authored stand-in for what a strengthened Bob loop would produce (see docs/CLAUDE_PROGRESS.md).",
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage --config vitest.config.ts",
    "test:strong": "vitest run --coverage --config vitest.strong.config.ts"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
