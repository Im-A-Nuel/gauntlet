# Gauntlet collaboration

Read `docs/AGENTS.md`, `docs/SCHEMA.md`, and `PROGRESS.md` before changing interfaces.

Codex owns the dashboard, shared documentation, integration QA, and Git. Claude Code session `gauntlet-claude` owns the Go CLI and demo repository. Claude records work in `docs/CLAUDE_PROGRESS.md`; Codex consolidates milestones in `PROGRESS.md`.

Use only Im-A-Nuel as commit author/committer with the existing configured email. Do not add AI coauthor trailers. Preserve unrelated user changes. Do not push without a verified project remote.

UI work uses ui-ux-pro-max, antislop, and antislop-ui during implementation. Check DESIGN.md for visual direction. Other requested visual skills are currently unavailable; record this rather than claiming to use them.
